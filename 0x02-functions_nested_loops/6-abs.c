#include "main.h"
#include <stdio.h>
/**
 * _abs - checks for absolute value
 * @t: parameter to be checked
 * Return: a (successful)
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
